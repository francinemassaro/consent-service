spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/consents_db
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  data:
    mongodb:
      uri: mongodb://localhost:27017/consents_audit

server:
  port: 8080